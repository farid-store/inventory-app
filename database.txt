CREATE TABLE barang (
    id INT AUTO_INCREMENT PRIMARY KEY,
    kode_barang VARCHAR(20),
    nama_barang VARCHAR(100),
    stok INT,
    harga INT
);

INSERT INTO barang(kode_barang, nama_barang, stok, harga)
VALUES
('HP001', 'Poco M3', 5, 450000),
('LT001', 'Lenovo ThinkPad', 2, 3500000);
